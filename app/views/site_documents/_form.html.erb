<%= form_for(@site_document, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @site_document.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@site_document.errors.count, "error") %> prohibited this site_document from being saved:</h4>

      <ul>
      <% @site_document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :name, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :documentable_type, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :documentable_type, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :documentable_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :documentable_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
